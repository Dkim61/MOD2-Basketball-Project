<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>There were some errors:</h2>

      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :user_name %>
  <%= f.text_area :user_name %><br>

  <%= f.label :full_name %>
  <%= f.text_area :full_name %><br>

  <%= f.label :email %>
  <%= f.text_area :email %><br>

  <%= f.label :age %>
  <%= f.number_field :age %><br>

  <%= f.submit %>

  <% end %>

